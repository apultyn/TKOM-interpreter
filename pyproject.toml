[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyscript-compiler"
version = "1.0.0"

[tool.setuptools.packages.find]
include = ["src*"]

[project.scripts]
psc = "src.interpreter.main:main"
